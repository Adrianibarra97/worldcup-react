import { render } from "@testing-library/react"
import { Footer } from "./Footer"

describe('Footer', () => {
  it('smoke test de footer.', () => {
    //Arrange: 
    render(<Footer/>)

    //Act:
    // Introducir valores en el input.

    //Assert:
    // Comprobar que insertValue tiene el mismo valor que seteamos.
  })
})